/*function setup() {
  createCanvas(1080, 480);
  frameRate(30);
}

function draw() {
  const SQUARE_SIZE = 60;

  for (var x = 0; x < gameState.length; x++) {
    for (var y = 0; y < gameState[x].length; y++) {
      const tile = gameState[x][y];
      setColor(tile);

      rect(
        SQUARE_SIZE * (x + 1),
        SQUARE_SIZE * (y + 1),
        SQUARE_SIZE,
        SQUARE_SIZE
      );
    }
  }
}

function setColor(tile) {
  switch (tile) {
    case 0:
      fill(255);
      break;
    case 1:
      var c = color(80, 0, 80);
      fill(c);
      break;
    default:
      fill(0);
  }
}

        18885209090
        broker@ups.com
      4S11V9HVYGX

*/
